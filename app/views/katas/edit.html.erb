<h1>Edit kata</h1>

<div>
<%= form_for(@kata) do |f| %>
<fieldset>
  <label for="title">Title</label>
  <%= f.text_field :title %>
  <label for="description">Description</label>
  <%= f.text_field :description %>
</fieldset>
<%= f.submit 'Save' %>
<% end %>
</div>
